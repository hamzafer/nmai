# Task Capture: Bank Reconciliation from CSV

**Timestamp:** 2026-03-21 18:57 (06:57 PM)
**Score:** 0/10 (0%)
**Duration:** 115.7s
**Submission #:** 12/300 today

## Prompt

> Avstem bankutskriften (vedlagt CSV) mot apne fakturaer i Tripletex. Match innbetalinger til kundefakturaer og utbetalinger til leverandorfakturaer. Handter delbetalinger korrekt.

**Translation (Norwegian):** Reconcile the bank statement (attached CSV) against open invoices in Tripletex. Match incoming payments to customer invoices and outgoing payments to supplier invoices. Handle partial payments correctly.

## Files

| Filename | MIME Type | Base64 Length |
|----------|-----------|---------------|
| files/bankutskrift_nb_01.csv | text/csv | 944 |

## Category

**BANK_RECONCILIATION_CSV** (Tier 3 — NEW task type, complex multi-step with file)

## API Plan

| # | Method | Path | Description | Status |
|---|--------|------|-------------|--------|
| 0 | GET | /invoice?... | Get all invoices | 200 (found some) |
| 1 | GET | /supplierInvoice?... | Get supplier invoices | 200 (empty) |
| 2-6 | GET | /invoice?invoiceNumber=1001..1005 | Find individual invoices | 200 (all empty — id=None) |
| 7-11 | PUT | /invoice/{id}/:createPayment | Register payments | SKIP (all unresolved refs) |
| 12-14 | GET | /supplierInvoice?supplierName=... | Find supplier invoices | 200 (all empty) |

## Fix Round

All 5 fix calls returned 404 — tried to register payments with guessed IDs.

## Error Analysis

1. **CSV file was read** — the agent extracted invoice numbers (1001-1005), customer names (Berg AS, Nilsen AS, etc.), and amounts from the CSV
2. **No matching invoices found** — GET /invoice?invoiceNumber=1001 returned empty. The sandbox has invoices but they don't match the CSV numbers
3. **The invoices in the sandbox are pre-existing** — need to search by amount, customer name, or date range instead of invoice number
4. **Supplier invoices also empty** — no supplier invoices exist in sandbox, may need to be created first
5. **All payments skipped** — because invoice lookups returned None IDs

## What Would Fix This

1. **Better invoice search strategy** — search by customer name, amount, or use the general list (first GET returned id=2147619700, which is an existing invoice)
2. **Parse CSV properly** — extract all transaction details and match by amount/date
3. **Handle case where invoices don't exist** — may need to create invoices from CSV data first
4. **Supplier invoice matching** — search differently, maybe by amount or org number

## Key Learning

This is a Tier 3 task — it requires:
- Reading CSV file content
- Understanding bank statement format
- Matching transactions to existing entities
- Handling partial payments
- Both customer and supplier invoice matching

## Raw Data

- Raw request: `tripletex/logs/raw/20260321_175734.json`
- Processed log: `tripletex/logs/20260321_175734.json`
